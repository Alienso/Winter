//
// Created by Alienson on 3.2.2024..
//

#include "Router.h"

void Router::routeRequest(shared_ptr<HttpRequest> &request) {
    /*for (auto endpoint : endpoints){
        if (endpoint.method == request->method && endpoint.url == request.uri){
            endpoint.fund(request);
        }
    }*/
}
